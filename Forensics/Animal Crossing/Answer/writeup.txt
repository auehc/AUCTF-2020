Filter all DNS queries
Export DNS queries to Packet Dissections
Use the regex query .{57}.ad.quickbrownfoxes.org to extract the DNS queries
delete the inappr records
Then filter out the duplicates while retaining the order using awk - cat data.txt | awk '!seen[$0]++'
base64 decode the text

Flag: auctf{it_was_star_wars_all_along}
